# Repository-scoped Codex configuration.
# Codex loads this file only after the project is trusted.

approval_policy = "on-request"
sandbox_mode = "workspace-write"

[agents]
# Keep translation/review fan-out bounded. Manifest writes must remain sequential.
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800
