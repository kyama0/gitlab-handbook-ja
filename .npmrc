# `@astrojs/check` peer-requires `typescript@^5.0.0`, but the project's
# devDependency was bumped to `typescript@^6.0.2` by Dependabot. There is no
# `@astrojs/check` release yet that supports TS 6, so accept the loose peer
# match instead of failing `npm ci` in CI.
legacy-peer-deps=true
